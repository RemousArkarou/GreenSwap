<Window
    x:Class="GreenSwap.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GreenSwap"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="GreenSwap">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>



    <Grid>


        <!-- Define row heights, Auto for header, * for content -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0"
      Background="LimeGreen"
      Padding="10">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Static app title -->
            <TextBlock Text="GreenSwap"
               Grid.Column="0"
               Foreground="White"
               FontSize="24"
               FontWeight="Bold"
               Margin="0,0,20,0"/>


            <!-- Dynamic page title -->
            <TextBlock x:Name="PageTitleText"
               Grid.Column="1"
               Foreground="White"
               FontSize="20"
               FontWeight="SemiBold"
               VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Grid>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Frame x:Name="contentFrame" Background="#1FB91C" />
        </Grid>
    </Grid>
</Window>