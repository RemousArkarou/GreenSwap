<Window
    x:Class="GreenSwap.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GreenSwap"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="GreenSwap">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <!-- Define row heights, Auto for header, * for content -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#FF333333" Padding="10">
            <TextBlock Text="GreenSwap" Foreground="White" FontSize="24" VerticalAlignment="Center"/>
            <Button Content="Home" Foreground="White" Margin="10,0" Background="Transparent" BorderBrush="Transparent"/>
            <Button Content="About" Foreground="White" Margin="10,0" Background="Transparent" BorderBrush="Transparent"/>
            <Button Content="Contact" Foreground="White" Margin="10,0" Background="Transparent" BorderBrush="Transparent"/>
        </StackPanel>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <!-- Your content goes here -->
        </Grid>
    </Grid>
</Window>