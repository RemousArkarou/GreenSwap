<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GreenSwap.Pages.Plant.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GreenSwap.Pages.Plant"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#1A1A1A">
        <ScrollViewer>
            <StackPanel Padding="20" Spacing="25">

                <Button
                    Content="← Back"
                    HorizontalAlignment="Left"
                    Background="#333333"
                    Foreground="White"
                    Padding="12,5"
                    CornerRadius="4"
                    Click="BackButton_Click"/>

                <Border
                    Padding="20"
                    CornerRadius="12"
                    Background="#252525" 
                    HorizontalAlignment="Left"
                    Width="320"
                    BorderBrush="#333333"
                    BorderThickness="1">

                    <StackPanel Spacing="15">
                        <TextBlock Text="Filters" FontSize="22" FontWeight="Bold" Foreground="#98FF98"/>

                        <TextBox x:Name="NameFilterBox" Header="Name" PlaceholderText="Search plants..." Background="#333333" Foreground="White"/>

                        <ComboBox x:Name="CategoryFilterBox" Header="Plant Type" HorizontalAlignment="Stretch" Background="#333333" Foreground="White">
                            <ComboBoxItem Content="All" IsSelected="True"/>
                            <ComboBoxItem Content="Kamerplant"/>
                            <ComboBoxItem Content="Vetplant"/>
                            <ComboBoxItem Content="Bloemplant"/>
                        </ComboBox>

                        <Button Content="Apply Filters" HorizontalAlignment="Stretch" Background="#98FF98" Foreground="Black" FontWeight="Bold" Height="40" CornerRadius="6" Click="FilterButton_Click"/>
                    </StackPanel>
                </Border>

                <ListView x:Name="ItemsListView" SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,15"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#98FF98" CornerRadius="12" Padding="15" RequestedTheme="Light">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="180"/>
                                    </Grid.ColumnDefinitions>

                                    <Border CornerRadius="8">
                                        <Image Source="{Binding Image}" Width="100" Height="100" Stretch="UniformToFill"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="20,0,20,0" VerticalAlignment="Center" Spacing="4">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="20" Foreground="Black"/>

                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <TextBlock Text="{Binding PlantType}" Foreground="#2E7D32" FontWeight="SemiBold" FontSize="14"/>
                                            <TextBlock Text="|" Foreground="#444444"/>
                                            <TextBlock Text="{Binding Rarity}" Foreground="#444444" FontSize="14" FontStyle="Italic"/>
                                        </StackPanel>

                                        <TextBlock Text="{Binding Description}" FontSize="13" Foreground="#333333" TextWrapping="Wrap" MaxLines="2"/>
                                        <TextBlock Text="{Binding SoilType}" FontSize="12" Foreground="#555555" FontWeight="Light"/>

                                        <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="Bold" Foreground="#1A1A1A" Margin="0,5,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="8">
                                        <StackPanel HorizontalAlignment="Right" Margin="0,0,5,5">
                                            <TextBlock Text="{Binding CreditPrice}" FontSize="24" FontWeight="Black" Foreground="Black" HorizontalAlignment="Right"/>
                                            <TextBlock Text="GREEN CREDITS" FontSize="10" FontWeight="Bold" Foreground="#2E7D32" HorizontalAlignment="Right" Margin="0,-5,0,0"/>
                                        </StackPanel>

                                        <Button Content="Details" HorizontalAlignment="Stretch" Background="White" Foreground="Black" CornerRadius="4" Click="DetailsButton_Click"/>
                                        <Button Content="Nu Ruilen" HorizontalAlignment="Stretch" Background="#1A1A1A" Foreground="White" FontWeight="Bold" CornerRadius="4" Click="TradeButton_Click"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>